# Instruction

Keep the scheduler's library repository in sync with its remote. Run the git-sync util on
this path (it commits, pulls, and pushes):

- ~/.local/share/routine-scheduler-libraries

Report whether it pulled updates, pushed, was already up to date, or hit a conflict.
Do not attempt to resolve conflicts — just report them.
