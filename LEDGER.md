

### self-audit:20260717-132609 — Shipped the F91 fix: conversations now carry instruction.md (their task) in the system prompt (0.67.2)
- **Anchor at start**: `c547b24` / 12:56Z. Window ~50min: **0 external scheduler commits** (HEAD stayed `c547b24`, tree clean), **0 new routine runs**. One reviewer `[AUDIT note]` + a mid-run retraction.
- **Health green**: pytest **995/3** (twice — before and after the version bump; no flake, F84 no recurrence); rsched-lint clean (all workflows/traits/permissions ok); journal unavailable (F62, `journalctl not found`). UI traces: SSE reconnect bursts on the run view (12:31–12:52, ~1/2min) but **no error events** — normal keepalive cadence, not a defect.
- **Reviewer feedback reconciled (1 [AUDIT note] + retraction)**:
  - The note asked, after the F91 fix, that a conversation's INITIAL instruction be *married to the best-fitting workflow (bounded by the turn setting)*, that EVERY later message re-select a fitting workflow (state machine possibly resuming mid-way), and be married again.
  - **Mid-run the user RETRACTED** the per-message re-marry: *"scratch that. we don't want a new workflow with each user message. but we want it to remain possible that the system provides a more complex multi-turn response."*
  - Reconciliation: the F91 fix authorized by *"Make sure the following is true after your fix for F91"* was implemented; the shipped conversation ownership prose **explicitly preserves multi-turn / sub-work replies**. The residual *marry-initial-task-to-best-fitting-workflow* is converse-**workflow** behaviour living in the **non-writable** library dir (routine-improver's domain) → tracked as **F92**, no scheduler-repo change.
- **Commit (test-gated green, 995/3)**:
  | commit | ver | what |
  |---|---|---|
  | `cab1eb26` | 0.67.2 | **F91**: `engine/composer.py` — added `_is_conversation(ctx)` (detects a conversation by HOME: dir under `ctx.server.conversations_home`, mirroring `daemon.runner._under_home`); `build_system_prompt` now carries the `# INSTRUCTION` section for a conversation at depth 0; `harness_contract` gained a 3rd ownership branch naming `instruction.md` as the task, framing later messages as refinements, and stating a reply may take several turns / spawn sub-work. +test `test_conversation_prompt_carries_its_first_message` (pos+neg). `docs/prompt-anatomy.md` §5 no longer "subrun-only". 5 files, +83/-12. |
- **MECHANISM CORRECTION vs last run's D17=B**: last run said branch on `ctx.routine.kind == "conversation"`. **That attribute does not exist** — `RoutineConfig` has no `kind` field and pydantic drops the yaml `kind: conversation` (confirmed by `runner.py:88` *"discriminated by HOME everywhere (cfg.kind is dropped by pydantic)"*). Root-cause verification also confirmed on a conversation's **first run** the inbox is empty AND the instruction is dropped (boot.py), so the agent truly saw no task — exactly the reported symptom. Premise checked against CLAUDE.md: the fix **aligns** with its stated *"the first message IS instruction.md"* and touches no contract (action schema / EVENT_TYPES / CLAUDE.md ownership untouched).
- **Findings**: **F91 (problem, FIXED)**, **F92 (improvement, new — converse-workflow marry, out of writable scope)**, F63 (systemic, carried — composer.py still under budget), F84 (info/watch — no recurrence), F62 (info, carried). **Dropped** settled F89 (fixed last run, one-cycle elapsed).
- **Decisions**: **D17 → settled** (implemented option B, mechanism corrected). No open decisions.
- **committed_code=true → restart REQUESTED** (sentinel dropped, brings 0.67.2 live). **Anchor** → `cab1eb265d5cc9cb66726a40122ea1bdee3d436c` / 2026-07-17T13:52Z. **Next run**: F92 is routine-improver's (library) — not this routine's to fix; watch the UI-flake class (F84); **cruft sweep now DUE** (last full sweep 2026-07-13, ~4 runs ago) — do a fresh-eyes whole-tree sweep next run.
